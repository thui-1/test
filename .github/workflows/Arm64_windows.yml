name: Windows ARM64 Runner
on: push

jobs:
  build:
    # Use the new Windows ARM64 runner label
    runs-on: windows-11-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Tmate (Debug Access)
        uses: mxschmitt/action-tmate@v3
